#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(void) {
  /*
  int n = 0, size = 100;
  int* A = (int*) malloc (size * sizeof(int));
  int x;
  while (1) {
    scanf("%d", &x);
    if (x == 0) {
      break;
    }

    if (n == size) {

    }
    A[n] = x;
    n++;
  }
  */
  printf("%d", sqrt((double) 5));
}
