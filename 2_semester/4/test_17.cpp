#include <stdio.h>
#include <stdlib.h>
#include <string>

int main() {
  long long w2, w1, w0;
  string my_res, fact_res;
  const long long two = 1 << 32, ten = 1000 * 1000 * 1000, MAX_INT = two - 1;

  for (int i = 0; i < 5; i++) {
    w2 = rand() * rand() * rand() % two;
  }
}
